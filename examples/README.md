# Examples

`requests/sample_request.json` is a single example `UserRequest` payload,
usable with the dry-run CLI:

```bash
python main.py --request-file examples/requests/sample_request.json --dry-run
```

No fixture in this folder represents real analysis output — the pipeline is
scaffold-only, so there is nothing to fake here.
